extends Main
block append scripts
  script(src="/js/single-logout.js")

block append content
  div#logout
    div.content-container
      h1 Du er ved at blive logget ud af dine bibliotekstjenester
      div#singlelogout
  div.iframes
  script var singleLogoutData = !{JSON.stringify({clients, redirect_uri, link, serviceName})}

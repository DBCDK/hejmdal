extends Main

block append content
  div.dialog(role="document")
    div.content
      div.header
        h4.title
          strong #{textObj.title}
      div.body
        each section in textObj.sections
          div
            strong #{section.header}
          each text in section.texts
            div !{text}
          div &nbsp;

  div &nbsp;

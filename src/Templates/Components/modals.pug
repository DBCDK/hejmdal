div#dimmer(onclick='onDimmerClick()')

if(help)
  include modals/modal-help

if(cookie)
  include modals/modal-cookie

if(privacyPolicy)
  include modals/modal-privacy

if(newUser)
  include modals/modal-user

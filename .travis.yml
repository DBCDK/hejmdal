language: node_js
sudo: false
dist: trusty
services:
  - postgresql
cache:
  bundler: true
  directories:
  - node_modules
branches:
  only:
  - master
  - oAuth2
script: npm run test
addons:
  postgresql: "9.4"
